# Capacitor Archive Safety Report — 2026-06-05

**Generated:** 2026-06-05 09:14 ICT
**Auditor:** Claude agent (inline scan, not via script — see note)
**Target to archive:** `app.muonnoi.org/mobile-shell/`

> Note: The shell script `scripts/verify-capacitor-archive-safe.sh` had performance issues scanning the entire monorepo (7000+ files in plays.muonnoi.org, corrupt backup dirs, multiple legacy clones). This report was generated by a targeted manual scan of active code paths only.

---

## Scan scope

**Active code dirs scanned:**
- `apps/mobile/` (React Native + Expo)
- `ai.muonnoi.org/workers/` (Cloudflare Worker API)
- `app.muonnoi.org/` (excluding `mobile-shell/` itself + `node_modules/`)
- `sites/dulich/`, `sites/hoctap/` (Astro scaffolds)

**Excluded:**
- `*.corrupt-2026-05-07/` (legacy backup dirs)
- `muonnoi-ai/`, `muonnoi-ai-machine/`, `muonnoi-app/`, `muonnoi-node/` (do-not-deploy clones, gitignored)
- `plays.muonnoi.org/` (7000+ generated HTML, no Capacitor refs by design)
- All `node_modules/`, `dist/`, `.pages-deploy/`

## Patterns scanned

- `from ['"]@capacitor` (ES imports)
- `import ['"]@capacitor` (dynamic imports)
- `require(['"]@capacitor` (CommonJS)
- `mobile-shell/` (path references)

---

## Findings

### Capacitor imports in active code: **0**

No file in `apps/mobile/`, `ai.muonnoi.org/workers/`, `app.muonnoi.org/` (outside mobile-shell), or `sites/` imports any `@capacitor/*` package.

### `mobile-shell/` path references in active code: **10 — all in markdown docs**

| File | Type |
|------|------|
| `apps/mobile/README.md` | Doc — references legacy for context |
| `docs/launch/DEV_TEAM_100_PERCENT_PLAN_2026-05-19.md` | Doc — verification spec |
| `docs/launch/MUONNOI_MOBILE_TEAM_HANDOFF_INDEX_2026-05-12.md` | Doc — handoff history |
| `docs/launch/MUONNOI_FINAL_EXECUTION_PLAN_2026-05-19.md` | Doc — release plan |
| `docs/launch/CREDENTIALS_NEEDED_2026-05-19.md` | Doc — credentials |
| `docs/launch/MUONNOI_MASTER_PLAN_STATUS_2026-05-11.md` | Doc — status snapshot |
| `docs/launch/SOLO_TEAM_RUNBOOK_TO_100_2026-05-19.md` | Doc — runbook |
| `lqos.muonnoi.org/LQOS_RELEASE_TRACKER_2026.md` | Doc — tracker |
| `lqos.muonnoi.org/MUONNOI_LQOS_UPGRADE_PROPOSAL_2026-06-05.md` | Doc — proposal |
| `lqos.muonnoi.org/LQOS_SPRINT_CURRENT.md` | Doc — sprint tracker |

All 10 are markdown — archiving `mobile-shell/` does NOT break any runtime functionality.

---

## Verdict: ✅ SAFE TO ARCHIVE

No active runtime code depends on `@capacitor/*` packages or `mobile-shell/` path outside the target directory itself.

### Proceed with archive command:

```bash
cd /Users/tranhatam/Documents/Devnewproject/muonnoi.org/app.muonnoi.org

# Archive in submodule (preserve git history)
mkdir -p _archive
git mv mobile-shell _archive/mobile-shell-capacitor-legacy-2026-06-05

git commit -m "chore: archive Capacitor mobile-shell legacy (per Founder R5 decision)

Verified by Claude agent 2026-06-05: no active code references
@capacitor imports outside the archived dir. 10 markdown docs reference
the legacy path, all non-runtime."

git push origin main

# Back in parent repo
cd ..
git add app.muonnoi.org
git commit -m "submodule: app.muonnoi.org pointer after mobile-shell archive"
git push origin main
```

### Impact on 9-condition tracker

**C3 iOS DEVELOPMENT_TEAM** currently checks `app.muonnoi.org/mobile-shell/ios/App/App.xcodeproj/project.pbxproj`. After archive, path moves to `_archive/...`.

Two options:

1. **Update tracker path** to check archived location (keeps tracking legacy iOS Capacitor signing)
2. **Retire C3** — Capacitor era is over; new mobile is RN+Expo (`apps/mobile/`); add NEW gate when RN iOS build ready

**Recommend Option 2** — Capacitor signing no longer valuable.

---

**Filed by:** Claude agent
**Status:** Ready for Founder approval to execute archive command above.
